#include <iostream>
#include <algorithm>
#include <vector>
#pragma warning(disable:4996)
#include <sstream>
#include <math.h>

#include <stack> // header file

using namespace std;

int main(int argc, const char * argv[]){
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    stack<int> s; // define
    
    s.push(data); // data 넣기
    
    s.pop(); // top 값 삭제
    
    s.top(); // stack top value
    
    s.size(); // size or length
    
    s.empty(); // if stack is empty >>  s.empty() == true,  if not >> s.empty() == false
    
    
    return 0;
}
